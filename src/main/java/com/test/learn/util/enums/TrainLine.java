package com.test.learn.util.enums;

public enum TrainLine {
    /**
     * Hardcoding the list of possible unique train lines for now, we can later modify the code to create the list of
     * unique lines dynamically
     */
    NS, EW, CG, NE, CC, CE, DT, TE
}
